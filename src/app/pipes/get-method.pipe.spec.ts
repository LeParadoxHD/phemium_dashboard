import { GetMethodPipe } from './get-method.pipe';

describe('GetMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMethodPipe();
    expect(pipe).toBeTruthy();
  });
});
