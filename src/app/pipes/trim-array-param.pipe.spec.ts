import { TrimArrayParamPipe } from './trim-array-param.pipe';

describe('TrimArrayParamPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimArrayParamPipe();
    expect(pipe).toBeTruthy();
  });
});
